# REVIEW CHECKLIST

- [ ] `python main.py` стартует без ошибок
- [ ] API отвечает на `http://localhost:8000`
- [ ] Все 4 эндпоинта работают (`/health`, `/shorten`, `/{code}`, `/stats/{code}`)
- [ ] Редирект по `GET /{code}` корректный
- [ ] Используется `.env` для конфигурации (порт, путь к БД) или эквивалент через CLI-аргументы
- [ ] `requirements.txt` корректно описывает зависимости
- [ ] Структура проекта соответствует заданию (`main.py`, `app/`, `db.py`, `tests/`)
- [ ] Корректная обработка 404

## Advanced

- [ ] Middleware логирует каждый запрос в `access.log`
- [ ] `GET /stats/dashboard` возвращает агрегированную статистику
